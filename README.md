# kill-switch
Private developer failsafe for contractual breach scenarios. A controlled termination protocol that permanently disables a PHP/MySQL website by wiping critical data, removing configuration access, and enforcing a hard lockout.
